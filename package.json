{
  "name": "expo-mcp-monorepo",
  "private": true,
  "workspaces": {
    "packages": ["packages/*"],
    "catalog": {
      "@modelcontextprotocol/sdk": "^1.17.5",
      "@types/bun": "^1.2.21",
      "@types/node": "^22.18.0",
      "eslint": "^9.34.0",
      "eslint-config-universe": "^15.0.3",
      "prettier": "^3.6.2",
      "typescript": "^5.9.2",
      "zod": "^3.25.76"
    }
  },
  "scripts": {
    "build": "bun run --filter \"@expo/mcp-tunnel\" --filter \"expo-mcp\" build",
    "lint": "bun run --filter \"@expo/mcp-tunnel\" --filter \"expo-mcp\" lint",
    "prepare": "bun run build"
  }
}
